import { ButtonGroup } from '@chakra-ui/react';

const ButtonContainer = ({ children }) => {
  return <ButtonGroup variant='solid'>{children}</ButtonGroup>;
};

export default ButtonContainer;
